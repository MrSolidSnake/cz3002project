<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mrsolidsnake.cz3002project">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Instruction_Page" />
        <activity android:name=".Mode_Selection_Screen" />
        <activity android:name=".mode_1_screen" />
        <activity android:name=".mode_2_screen" />
        <activity android:name=".mode1_result" />
        <activity android:name=".upload_photo" />
        <activity android:name=".camera_mode" />
        <activity android:name=".load_fromdirmode"></activity>
    </application>

</manifest>
